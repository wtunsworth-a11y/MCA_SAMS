# incoming/

Drop folder. Copy files you want me to look at into this directory.

Anything here is treated as source material — reference documents, exported
survey definitions, photo schedules, spreadsheets, screenshots — not as part of
the application itself. Nothing in this folder is loaded by the app at runtime.

This README exists so git tracks the (otherwise empty) folder. Leave it in place.
